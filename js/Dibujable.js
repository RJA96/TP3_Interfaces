export class Dibujable {
    constructor(x, y,speed,canvasWidth,canvasHeight) {
        this.x = x;
        this.y = y;
        this.speed = speed;
        this.canvasWidth = canvasWidth;
        this.canvasHeight = canvasHeight;
    }
    draw(){
    }
}